version: "3.8"

services:
  backend:
    build: ./backend
    volumes:
      - ./backend/logs:/app/logs
    networks:
      - app-network

  frontend:
    build: ./frontend
    environment:
      - VITE_API_URL=http://backend:3000
    networks:
      - app-network

networks:
  app-network:
